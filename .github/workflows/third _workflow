name: Third  Workflow

on: [push]
 
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      
      - name: Set up in Multiline
        run: |
          pwd
          ls -a
- name: Set up in Multiline
        run: |
          pwd
          whoami
          
