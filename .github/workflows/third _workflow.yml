name: Third  Workflow

on: [push]
 
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      
      - name: List files amd Copy Repo
        run: ls -a
      - uses: actions/checkout@v4
      - name: List files 
        run: ls -a
          
